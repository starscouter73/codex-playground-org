plugins {
    id 'com.android.application'
}

android {
    namespace 'de.markdorth.ausgaben'
    compileSdk 35

    defaultConfig {
        applicationId 'de.markdorth.ausgaben'
        minSdk 29
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
